# ai-preview
A full-stack AI-powered UI Preview Generator built using Angular and FastAPI.  The application accepts natural language prompts and dynamically generates UI styles rendered in real time.
